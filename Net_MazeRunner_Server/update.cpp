#include"stdafx.h"


void UpdateShaderResources()
{
	
	//	//1) 클라이언트로부터 받아 온 player의 정보, map의 변경점을 반영해 변수를 변경.
	//	//2) 각 객체가 가지고 있는 update를 진행하고,
	//	//3) 충돌체크로 일어나는 좌표 변경점이나, 블록타입 변경을 결정
	//	//4) 그렇게 변경된 부분을 클라이언트로 전송 하는 부분을 포함
	//	CPlayer GetClientPlayer//전송 받은 플레이어 값 저장 
	//		CMap GetServerMap//전송 받은 맵 값 저장
	//		RegistPlayerInfo(CPlayer GetClientPlayer)
	//		MapChanged(CMap GetServerMap)
	//
	//Collision(GetClientPlayer, Map)
	//	Ghost//유령의 움직임
	//	Int GhostChasePlayer//쫒을 플레이어
	//	Int Len//플레이어간 거리
	//	For i from 0 to 4
	//	If COtherPlayer[i] to ghost len < len
	//	Len = ghost len
	//	GhostChasePlayer = i

	//SMap_Info.update()
	//for(int i = 0;i<4;++i)
	//SPlayer_info.update()
	




	//Collision();
}